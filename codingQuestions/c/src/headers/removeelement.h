#ifndef REMOVEELEMENT_H
#define REMOVEELEMENT_H

/**
 * Remove all elements from the array with the value val input.
 * 
 * Modify nums array such that the first "k" elements are those not equal to "val"
 *  and return that "k" value
 */
int removeElement(int* nums, int numsSize, int val);

#endif