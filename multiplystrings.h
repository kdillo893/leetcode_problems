#ifndef MULTSTRINGS_H
#define MULTSTRINGS_H

/**
 * multiply strings which represent numbers. Don't use any "int parsing" libraries or multiplying by using ints.
 */
char* multiply(char * num1, char * num2);

#endif